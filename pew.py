from tkinter import*

	def iCalc(source, side):
storeObj = Frame(source, borderwidth = 1, bd=4 bg="powder blue")
storeObj.pack(side=side, expand=YES, fill=BOTH)
return storeObj

	def button  (source , side, text, commander = None):
storeObj.pack(side=side, expand=YES, fill=BOTH)
return storeObj

class app(Frame): 
def _init_(self():
	Frame._init_(self)
	slef.option_add('*Font', 'arial 20 bold')
	self.pack(expand=YES, fill=BOTH)
	self.master.title('Calculator')	
	
	if_name_ == '_main_':